section.section-questions
  .section-questions--content
    p.intro
      | 8/28，台灣即將舉行核四公投，但你真的了解核四嗎？ <br />
      | 試試下面這個小測驗， 看看核四是否跟你想像中的一樣！

    h1 #{greeting}

    div.splide.question-slider
      div.splide__track
        ul.splide__list

          - lastIndex = questions.length - 1
          each item, i in questions
            li.splide__slide
              div.question.card.question-box(data-answer=`a${item.answer}`)
                p Q#{i + 1}.#{item.question}
                div.option-list
                  each option, j in item.options
                    label.button(for=`q${i}-a${j}`)
                      if j === item.answer
                        img(src="../images/question_yes.svg", width="21", height="22")
                      else
                        img(src="../images/question_no.svg", width="18", height="17")
                      | #{option}
                      input(type="radio", name=`q${i}`, id=`q${i}-a${j}`, value=`a${j}`)

            li.splide__slide
              div.card.answer-detail
                p.button #{item.options[item.answer]}
                p #{item.detail}
                if i !== lastIndex
                  div.btn-holder
                    button.button-black 下一題
